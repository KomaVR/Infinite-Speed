{
  "name": "video-editor",
  "version": "1.0.0",
  "description": "A simple video editor with speed control and splitting functionality",
  "main": "index.html",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "^3.0.0"
  },
  "engines": {
    "node": "16.x"
  },
  "vercel": {
    "builds": [
      {
        "src": "index.html",
        "use": "@vercel/static"
      }
    ]
  }
}
